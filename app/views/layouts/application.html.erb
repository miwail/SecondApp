<!DOCTYPE html>
<html>
  <head>
    <title>Todo list</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">

      <nav class="navbar navbar-inverse">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand"><%=link_to "Home", root_url%></a>
          </div>

          <%if loged_in?%>
            <a class="navbar-brand"><%=link_to "Profile", profile_path(current_user.profile) if current_user.profile %></a>
            <a class="navbar-brand"><%=link_to "My tasks", user_tasks_path(current_user)%></a>
            <a class="navbar-brand"><%=link_to "Add new task", new_user_task_path(current_user), method: :get%></a>
            <a class="navbar-brand"><%=link_to "Log out", logout_path, method: :delete%></a>
            <p class="navbar-text">Signed in as: <%=current_user.username%></p>

          <%else%>
            <p class="navbar-text"><%=link_to "Sign Up", signup_path %></p>
            <p class="navbar-text"><%=link_to "Sign In", login_path %></p>
          <%end%>
        </div>
      </nav>


      <div class="jumbotron">
        <h1 align='center'>ToDo list by Zol</h1>
      </div>
      <% flash.each do |key, value| %>
          <div class="<%= flash_class(key) %>">
                <%= value %>
          </div>
    <% end %>
    <%= yield %>
    <footer>
      <p align = 'center'>Created by     <%= link_to "Zol", "https://github.com/miwail/ror/" %></p>
    </footer>
    </div>
  </body>
</html>
